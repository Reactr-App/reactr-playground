import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Note: This demo project is designed for client-side rendering only
  // All pages use 'use client' directive for client-side rendering
  // This keeps demos simple and focused on UI/UX exploration
  // No SSR is needed for static demo experiences
};

export default nextConfig;
